<?xml version="1.0" encoding="utf-8"?>
<!--
  4x4 大组件：FrameLayout 容器内放 ListView + empty TextView，
  使 RemoteViews.setEmptyView 能正常切换可见性。
  容器 ID 仍是 widget_root；ListView 用系统约定的 @android:id/list。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="10dp">

    <TextView
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_text_secondary"
        android:textSize="11sp"
        android:singleLine="true"
        android:ellipsize="end"
        tools:text="All providers"
        xmlns:tools="http://schemas.android.com/tools" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp">

        <ListView
            android:id="@android:id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="2dp" />

        <TextView
            android:id="@android:id/empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textColor="@color/widget_text_muted"
            android:textSize="12sp"
            android:text="@string/widget_no_provider"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/widget_updated_at"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_text_muted"
        android:textSize="10sp"
        tools:text="更新于 12:34"
        xmlns:tools="http://schemas.android.com/tools" />
</LinearLayout>