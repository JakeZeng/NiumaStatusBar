# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# For more details, visit https://docs.gradle.org/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/android-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# classes declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
android.nonFinalResIds=false

# Proxy configuration for Gradle (disabled; start your local proxy on 18080 and uncomment if needed)
#systemProp.http.proxyHost=127.0.0.1
#systemProp.http.proxyPort=18080
#systemProp.https.proxyHost=127.0.0.1
#systemProp.https.proxyPort=18080
#systemProp.http.nonProxyHosts=localhost|127.0.0.1

# 真机（小米等 ARM 设备）需要 arm 目标；x86_64 保留给 MuMu 模拟器。
# 仅 x86_64 会导致 ARM 设备安装后直接闪退（找不到 native lib）。
abiList=arm64-v8a,armeabi-v7a,x86_64
archList=arm64-v8a,armeabi-v7a,x86_64
targetList=aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android
